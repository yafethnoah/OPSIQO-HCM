# GitHub production branch controls

Before production rollout, protect the release/certification branch and require successful quality, security, supply-chain and production-evidence checks. Disable force-push and deletion, require reviewed changes, and use a controlled promotion workflow. The repository connector cannot enforce these repository-admin settings automatically; they must be applied in GitHub settings by an authorized administrator.

import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { useBootstrap } from '@/hooks/use-bootstrap';
import { Card, H1, H2, Muted } from '@/components/ui';
import { colors } from '@/theme/tokens';
export default function Team(){const{data}=useBootstrap();if(!data?.capabilities.managerMode)return <View style={s.center}><Card><H2>Manager mode</H2><Muted>Your current OPSIQO membership does not include manager mobile access.</Muted></Card></View>;const m=data.manager||{};return <ScrollView style={{backgroundColor:colors.bg}} contentContainerStyle={s.content}><H1>My team</H1><View style={s.metrics}><Metric label="Team" value={m.teamSize??data.team.length}/><Metric label="Leave" value={m.pendingLeaveApprovals??0}/><Metric label="Timesheets" value={m.submittedTimesheets??0}/></View><Card><H2>Team members</H2>{data.team.length?data.team.map((w:any)=><View key={w.workerId} style={s.row}><Text style={s.title}>{w.displayName}</Text><Muted>{w.positionTitle||'Employee'}{w.orgUnitName?` · ${w.orgUnitName}`:''}</Muted></View>):<Muted>No team members are in your mobile scope.</Muted>}</Card><Card><H2>Manager safeguards</H2><Muted>H47.1 gives managers visibility only. Leave, time, expense, performance, and employment approvals remain in their governed OPSIQO workflows and are not silently automated by the mobile app.</Muted></Card></ScrollView>}
function Metric({label,value}:{label:string;value:any}){return <View style={s.metric}><Text style={s.metricLabel}>{label}</Text><Text style={s.metricValue}>{String(value)}</Text></View>}
const s=StyleSheet.create({content:{padding:18,gap:14},center:{flex:1,justifyContent:'center',padding:18,backgroundColor:colors.bg},metrics:{flexDirection:'row',gap:9},metric:{flex:1,backgroundColor:'#fff',borderWidth:1,borderColor:colors.line,borderRadius:14,padding:12},metricLabel:{fontSize:11,color:colors.muted},metricValue:{fontSize:22,fontWeight:'900',color:colors.text},row:{paddingVertical:10,borderBottomWidth:1,borderBottomColor:colors.line},title:{fontSize:15,fontWeight:'800',color:colors.text}});

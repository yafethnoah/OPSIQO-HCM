import { describe,it,expect } from 'vitest';import { blockedUse,validateAnswer,evidenceCompleteness } from '@/lib/ai-intelligence/governance';
const evidence=[{id:'e1',kind:'metric' as const,title:'Headcount',summary:'Headcount 100',source:'snapshot',asOf:new Date().toISOString(),dataQuality:'quality 90',sensitivity:'aggregate' as const}];
describe('AI HR governance',()=>{it('blocks individual termination selection',()=>expect(blockedUse('Which employee should we fire this month?')).toBeTruthy());it('allows aggregate workforce analysis',()=>expect(blockedUse('What workforce risks are visible in our current data?')).toBeUndefined());it('drops hallucinated citations and bounds confidence',()=>{const x=validateAnswer({summary:'x',findings:[],citations:[{evidenceId:'fake',claim:'x'},{evidenceId:'e1',claim:'ok'}],recommendations:[{id:'r',type:'review',title:'Review',rationale:'x',evidenceIds:['fake','e1'],priority:'medium'}],limitations:[],confidence:99,evidenceCompleteness:100,requiresHumanDecision:true},evidence);expect(x.citations).toHaveLength(1);expect(x.recommendations[0]?.evidenceIds).toEqual(['e1']);expect(x.confidence).toBeLessThanOrEqual(evidenceCompleteness(evidence));});});

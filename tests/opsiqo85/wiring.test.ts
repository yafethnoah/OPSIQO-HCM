import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';const r=(p:string)=>readFileSync(p,'utf8');describe('OPSIQO 8.5 product wiring',()=>{it('wires Gantt and AI progress into live workspaces',()=>{expect(r('src/components/workflow-panel.tsx')).toContain('GovernedGantt');expect(r('src/components/ai-copilot-workspace.tsx')).toContain('OpsiQoOperationProgress')});it('uses server-issued employee preview IDs',()=>{const route=r('src/app/api/organizations/[orgId]/imports/employees/route.ts');expect(route).toContain('previewId');expect(route).not.toContain('readyRows')});it('keeps cloud connector claims truthful',()=>{const ui=r('src/components/import-center-workspace.tsx');expect(ui).toContain('Google Drive');expect(ui).toContain('Not configured')})});

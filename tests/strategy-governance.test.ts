import { describe,expect,it } from 'vitest';import { capabilityRisk,readinessLevel,scenarioScore } from '@/lib/strategy/risk';
describe('human-capital strategy governance',()=>{it('escalates material capability gaps deterministically',()=>{expect(capabilityRisk(5,5,1,5).level).toBe('critical');expect(capabilityRisk(1,1,4,4).level).toBe('low')});it('rewards benefit/capability while penalizing risk',()=>{expect(scenarioScore(5,1,40)).toBeGreaterThan(scenarioScore(2,5,0))});it('bounds readiness levels',()=>{expect(readinessLevel(49)).toBe('emerging');expect(readinessLevel(70)).toBe('aligned');expect(readinessLevel(85)).toBe('strategic')})});

# OPSIQO HCM v3.6 Validation Report — Production Evidence Closure & Cloud DR Execution

Validation date: **2026-08-11**
Package version: **3.6.0**

## Release status
**Source/integration validated; production evidence closure remains blocked until a reviewed `package-lock.json` is generated and the protected network/cloud CI workflow is executed successfully.**

This report deliberately separates controls validated from source/runtime fixtures in this environment from evidence that requires the real npm registry, deployed OPSIQO environment, Google Cloud project, protected GitHub environment and independently reviewed DR exercise.

## Exact source/static validation
- TypeScript / TSX source files parsed under `src`, `scripts` and `tests`: **731**
- TypeScript parser diagnostics: **0**
- Project-local imports inspected: **1,837**
- Unresolved project-local imports: **0**
- Targeted v3.6 production-evidence/readiness/scripts TypeScript semantic diagnostics under local Node stubs: **0**
- Firestore Rules brace structure: **638 / 638 PASS**
- Storage Rules brace structure: **5 / 5 PASS**
- GitHub workflow YAML files parsed: **7 / 7 PASS**
- Bounded static source/secret scan: **PASS**

## v3.6 production-evidence engine runtime validation
Direct runtime validation of `src/lib/platform-reliability/production-evidence.ts` and the emitted evidence-capture scripts confirmed:
- Firestore PITR enabled + backup schedule present + fresh READY backup: **PASS**
- Required PITR/scheduled backup absent: **FAIL CLOSED**
- READY backup belonging to a different Firestore database: **EXCLUDED / FAIL CLOSED**
- Controlled DR fixture: **PASS**
  - measured RTO: **40 minutes**
  - measured RPO: **60 minutes**
  - sentinel verified: **true**
  - cleanup completed: **true**
- RTO/RPO target breach fixture: **not PASS**
- Production evidence bundle with every required gate/evidence status passing: **READY**
- Production readiness using `service_account` Firebase Admin mode: **PASS** under complete synthetic production configuration
- Production readiness using `adc` Firebase Admin mode without client email/private key: **PASS** under complete synthetic production configuration
- Production readiness with v3.6 CI/cloud-DR/restore evidence references removed: **FAIL CLOSED** on all three evidence-reference checks
- v3.6 static release gate from the finalized source: **37 / 38 PASS**; only `package-lock.json` fails.
- Production preflight under a complete synthetic ADC production configuration: **26 / 27 non-failing**; only the reviewed lockfile check fails.

These are deterministic local tests. They do **not** prove the real production project has PITR/backups configured or that a real restore will meet RTO/RPO.

## Cloud/CI evidence architecture verified in source
- Manual Cloud DR metadata workflow exists and uses Google GitHub Actions OIDC/Workload Identity Federation authentication.
- Protected production-evidence closure workflow requires the explicit `RESTORE_TEST` authorization input.
- The closure workflow requires a reviewed lockfile/source manifest before installation.
- Dedicated Gitleaks and CodeQL gates are included in the closure workflow.
- The workflow runs `npm ci`, `npm audit`, bounded static scan, SBOM generation, release gate, typecheck, tests, Firestore Rules tests, build, AI evaluation/governance, production preflight and integrated UAT.
- Firestore metadata capture, latest READY backup selection, restore to a new temporary database, sentinel SHA-256 verification, measured RTO/RPO, temporary-database deletion and evidence-bundle generation are represented in the protected workflow.
- Build provenance/SBOM release labels now derive from the actual package/release version instead of the inherited v3.5 constant.
- Synthetic lockfile/artifact execution confirmed the generated CycloneDX metadata and provenance external parameter both identify **3.6.0**, while local provenance retains `signed=false` and `slsaLevelClaimed=false`.
- Firebase Admin explicitly supports `service_account` or `adc` mode; readiness validates the selected mode.
- Production preflight delegates to the shared fail-closed readiness engine and identifies release 3.6.0.

## Important inherited defects corrected in v3.6
- Replaced the stale production preflight that still identified release **3.0.0**.
- Replaced the readiness default release version **3.5.0** with **3.6.0**.
- Added explicit ADC/Application Default Credentials support for Firebase Admin instead of requiring only a long-lived service-account key pair.
- Replaced the hard-coded SBOM generator version **3.5.0** with the actual package version.
- Replaced the hard-coded provenance release version **3.5.0** with the release/package version.
- Updated active navigation, Platform Reliability UI, Enterprise Command Center release form and public health metadata to **v3.6 / 3.6.0**.

## Dependency/build gate
A bounded public-registry command was attempted:

```text
npm install --package-lock-only --ignore-scripts --no-audit --no-fund --registry=https://registry.npmjs.org
```

The process exceeded the 25-second execution window (`timeout` exit code **124**) and produced:
- `package-lock.json`: **NO**
- `node_modules`: **NO**

No partial dependency/build state is retained in the release tree.

Therefore these gates are **NOT VERIFIED in this environment**:
- trusted `npm ci` from a reviewed lockfile;
- real package dependency typecheck;
- complete Vitest/unit/integration suite;
- Firebase Emulator Rules tests;
- Next.js production build;
- dedicated Gitleaks/CodeQL execution against the committed release in protected CI;
- deployed authenticated lifecycle UAT;
- real Google Cloud PITR/backup evidence capture;
- real Firestore restore into a temporary database;
- real sentinel integrity verification;
- measured production-project RTO/RPO;
- final CI production-evidence bundle readiness.

The protected v3.6 workflow is designed to produce and bind those missing pieces to one source commit/CI run once executed in an approved network/cloud environment.

## Production decision boundary
A green v3.6 evidence bundle is release-specific operational evidence. It is **not** a certification of legal compliance, security, SLSA level, permanent availability, complete data recoverability or guaranteed future RTO/RPO performance. A restore exercise applies only to the backup, database, sentinel, external dependencies and execution path actually tested.
## Release integrity
- Source-manifest entries (manifest excludes itself): **835**
- Source-manifest verification: **835 / 835 PASS**
- ZIP file entries including `SOURCE_MANIFEST.sha256`: **836**
- ZIP internal source-manifest mismatches: **0**
- ZIP compressed-data integrity: **PASS**
- Shipped dependency/build state: **no `package-lock.json`, no `node_modules`, no `.next`, no TypeScript build cache, no generated evidence-artifact directory**


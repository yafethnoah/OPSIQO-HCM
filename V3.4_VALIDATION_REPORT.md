# OPSIQO HCM v3.4 Validation Report

## Release
- Package: **OPSIQO HCM v3.4.0**
- Capability: **Enterprise Security Operations, Zero-Trust Access & HCM Cyber Resilience**
- Validation classification: **source/integration validated; dependency-build verification remains blocked by the unavailable trusted npm lockfile**.

## Exact source/static validation
- TypeScript / TSX source files parsed under `src`, `scripts`, and `tests`: **689**
- TypeScript parser diagnostics: **0**
- Project-local imports inspected (relative + `@/`): **1,880**
- Unresolved project-local imports: **0**
- Material v3.4 semantic diagnostics after excluding diagnostics caused solely by the intentionally unavailable external dependency/type tree: **0**
- Security Operations organization/API routes plus signed external ingress route: **15**
- Direct route imports into recruiting/compensation/separation/succession/performance/onboarding/worker consequential-mutation services: **0**
- `package.json`, `firebase.json`, `firestore.indexes.json`, `tsconfig.json`: **PASS**
- Firestore Rules brace structure: **600 / 600 PASS**
- Storage Rules brace structure: **5 / 5 PASS**
- Security Operations server-only Firestore collections/indexes: **14 / 14 PASS**

## RBAC / segregation
Verified from the shipped role-permission map:
- HR Partner has `securityops.read/manage/audit`: **PASS**
- HR Partner does **not** have `securityops.approve`: **PASS**
- Manager does not have enterprise `securityops.read`: **PASS**
- Employee does not have enterprise `securityops.read`: **PASS**

Security approval does not directly grant HCM membership/role authority. Approved privileged-access requests hand off to a separately governed Identity Hub access request; downstream identity approval/execution remains independent.

## Signed SOC/SIEM ingress hardening
Shipped source gates verify:
- HMAC-SHA256 over `timestamp + "." + exactRawBody`.
- Five-minute signed freshness window.
- 1 MiB inbound body ceiling.
- Per-organization secret map support.
- Single-tenant secret must be explicitly bound to one organization in production.
- Server-only signature-hash index; raw ingress signature/body is not persisted as SecurityEvent evidence.
- Short processing lease prevents concurrent duplicate processing.
- A completed duplicate resolves idempotently to the existing event.
- Rejected processing can be retried while the signed timestamp is still valid.
- Replay/signature indexes carry expiry and the security governance job removes expired entries in bounded batches.

Direct cryptographic runtime checks:
- Valid signature: **PASS**
- `sha256=`-prefixed valid signature: **PASS**
- Tampered body: **REJECTED**
- Malformed signature: **REJECTED**

## SIEM/SOC outbound boundary
- Export requires `securityops.approve` plus `integration.manage`.
- Export requires an **active `rest_push` Integration Runtime profile**.
- Export requires an active REST/JSON connector with a governed base URL and outbound contract.
- Production independently checks the connector host against exact `OPSIQO_SECURITY_SIEM_HOSTS` values, in addition to the Integration Runtime transport controls.
- Export staging contains bounded event metadata and expressly omits raw IP/session/credential/token material.

## Deterministic security posture runtime
Direct pure-runtime checks:
- Healthy posture: **100 / 100 — Resilient**
- Stress profile (critical incidents, high/critical events, privileged MFA gaps, overdue rotations/recertification and session anomaly candidates): **12 / 100 — Fragile**

The score is an operational cyber-risk indicator only. It does not authorize employment, discipline, performance, compensation, succession or other consequential HR decisions.

## Production readiness fail-closed checks
Synthetic production environment checks:
- Security Operations enabled with no ingest secret/binding and no SIEM allow-list: **FAIL CLOSED**
- >=32-character single-tenant secret + explicit organization binding + exact SIEM host allow-list: **PASS**
- Valid per-organization secret map + exact SIEM host allow-list: **PASS**
- Returned release version: **3.4.0**

## Integrated product controls
- Security Command Center route: **present**
- Security dashboard in Lifecycle UAT: **present**
- Identity Hub retained in Lifecycle UAT: **present**
- Enterprise HCM risk node/dependencies: **present**
- Aggregate-only Security Operations AI evidence kind: **present**
- `security:review` governance job: **present**
- v3.4 manual production-promotion workflow/readiness environment controls: **present**

## Release gate
The finalized source release gate was executed using the available global TypeScript runtime:
- **22 / 23 PASS**
- Sole failure: **`package-lock.json` absent**

The gate includes source/config/rules/runbook/acceptance checks, retained Identity and Integration services, Security Operations service/risk/ingress/cryptography/SIEM route/UI/tests, production-promotion workflow, UAT routes, package version, Node engine and governance jobs.

## Dependency-aware build gate
A bounded public-registry lockfile attempt was made:

```text
npm install --package-lock-only --ignore-scripts --no-audit --no-fund --registry=https://registry.npmjs.org
```

The command exceeded the 25-second execution window and produced:
- `package-lock.json`: **not produced**
- `node_modules`: **not produced**
- `.next`: **not produced**

Therefore the release does **not** claim that `npm ci`, the complete dependency-aware `tsc`, Vitest suite, Firebase Rules emulator suite, or Next.js production build passed. Those remain mandatory production-promotion gates in an approved networked Windows/CI environment.

## Release integrity procedure
Before packaging, generated dependency/build state and TypeScript build caches are removed. `SOURCE_MANIFEST.sha256` is regenerated only after all v3.4 source and release documents are final, then every manifest entry and the ZIP compressed-data integrity are reverified.

import{z}from'zod';export const envelopeSchema=z.object({documentId:z.string().trim().min(1).max(160),providerCode:z.string().trim().min(2).max(80).default('evidence_manual'),subject:z.string().trim().min(2).max(250),documentHash:z.string().regex(/^[a-fA-F0-9]{64}$/),signers:z.array(z.object({name:z.string().trim().min(1).max(160),email:z.string().email().max(320),role:z.string().trim().min(1).max(80)})).min(1).max(20)});

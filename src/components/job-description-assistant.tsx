'use client';
import { useState } from 'react';
import { activeOrgId, apiFetch } from '@/lib/http/client';
export function JobDescriptionAssistant({formId}:{formId:string}){const[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function analyze(){const form=document.getElementById(formId);if(!(form instanceof HTMLFormElement))return;const desc=form.elements.namedItem('description');if(!(desc instanceof HTMLTextAreaElement)||desc.value.trim().length<80){setMessage('Add the role description first.');return}setBusy(true);setMessage('');try{const r=await apiFetch<{data:{requirements:string[];preferredQualifications:string[];skills:string[];requiredYears?:number;warnings:string[]}}>(`/api/organizations/${activeOrgId()}/recruiting/ats/job-description`,{method:'POST',body:JSON.stringify({text:desc.value})});const req=form.elements.namedItem('requirements');if(req instanceof HTMLTextAreaElement){const merged=[...new Set([...req.value.split('\n').map(x=>x.trim()).filter(Boolean),...r.data.requirements])];req.value=merged.join('\n');req.dispatchEvent(new Event('input',{bubbles:true}));}setMessage(`Detected ${r.data.requirements.length} job-related requirement(s) and ${r.data.skills.length} role signals. Review the extracted criteria before creating the requisition.${r.data.warnings[0]?` ${r.data.warnings[0]}`:''}`);}catch(e){setMessage(e instanceof Error?e.message:'Job-description analysis failed.')}finally{setBusy(false)}}return <div className="atsIntakeBox stack"><div className="toolbar"><div><strong>Job-description intelligence</strong><div className="muted">Extract explicit job-related requirements to make ATS comparisons more explainable.</div></div><button type="button" className="button secondary compact" disabled={busy} onClick={analyze}>{busy?'Analyzing…':'Extract requirements'}</button></div>{message&&<div className="muted">{message}</div>}</div>}

'use client';import{useEffect,useState}from'react';import{activeOrgId,apiFetch}from'@/lib/http/client';import{LoadingState}from'@/components/data-states';export function PremiumHcmWorkspace(){const[d,setD]=useState<any>(null);useEffect(()=>{apiFetch<any>(`/api/organizations/${activeOrgId()}/premium-hcm/status`).then(x=>setD(x.data))},[]);if(!d)return <LoadingState label="Loading Premium HCM evidence map…"/>;return <div className="stack"><section className="card tableWrap"><h2 className="sectionTitle">Recommendation implementation map</h2><table><thead><tr><th>Recommendation</th><th>Implementation state</th></tr></thead><tbody>{d.recommendations.map((x:any)=><tr key={x[0]}><td>{x[1]}</td><td><span className="badge">{x[2]}</span></td></tr>)}</tbody></table></section><section className="card"><h2 className="sectionTitle">Evidence weights</h2><pre>{JSON.stringify(d.evidenceWeights,null,2)}</pre></section><div className="notice">{d.certificationRule}<br/>{d.regulatedPayrollRule}</div></div>}

import {NextResponse} from 'next/server';import {verifyAppCheckRequest} from '@/lib/auth/session';import {apiErrorResponse} from '@/lib/http/errors';import {enforceCandidatePortalRateLimit,submitPublicCandidateApplication} from '@/lib/recruiting/candidate-portal-service';
export async function POST(r:Request,c:{params:Promise<{token:string}>}){try{await verifyAppCheckRequest(r);const{token}=await c.params;await enforceCandidatePortalRateLimit(token,r,'submit');return NextResponse.json({data:await submitPublicCandidateApplication(token,await r.formData())},{status:201})}catch(e){return apiErrorResponse(e)}}

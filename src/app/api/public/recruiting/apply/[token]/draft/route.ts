import {NextResponse} from 'next/server';import {verifyAppCheckRequest} from '@/lib/auth/session';import {apiErrorResponse} from '@/lib/http/errors';import {enforceCandidatePortalRateLimit,loadPublicApplicationDraft,savePublicApplicationDraft} from '@/lib/recruiting/candidate-portal-service';
export async function GET(r:Request,c:{params:Promise<{token:string}>}){try{await verifyAppCheckRequest(r);const{token}=await c.params;await enforceCandidatePortalRateLimit(token,r,'draft');return NextResponse.json({data:await loadPublicApplicationDraft(token,r.headers.get('x-application-draft-token')||'')})}catch(e){return apiErrorResponse(e)}}
export async function POST(r:Request,c:{params:Promise<{token:string}>}){try{await verifyAppCheckRequest(r);const{token}=await c.params;await enforceCandidatePortalRateLimit(token,r,'draft');return NextResponse.json({data:await savePublicApplicationDraft(token,await r.json(),r.headers.get('x-application-draft-token')||undefined)})}catch(e){return apiErrorResponse(e)}}

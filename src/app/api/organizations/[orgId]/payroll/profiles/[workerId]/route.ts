import{NextResponse}from'next/server';import{actorFromRequest}from'@/lib/auth/session';import{apiErrorResponse}from'@/lib/http/errors';import{savePayrollWorkerProfile}from'@/lib/payroll/control-plane';
export async function PUT(r:Request,c:{params:Promise<{orgId:string;workerId:string}>}){try{const{orgId,workerId}=await c.params,a=await actorFromRequest(r,orgId);return NextResponse.json({data:await savePayrollWorkerProfile(a,workerId,await r.json())})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { submitExitInterview } from '@/lib/separation/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;caseId:string}>}){try{const{orgId,caseId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'separation.read');return NextResponse.json({data:await submitExitInterview(actor,caseId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

import {NextResponse} from 'next/server';import {actorFromRequest,requirePermission} from '@/lib/auth/session';import {apiErrorResponse} from '@/lib/http/errors';import {createCandidateApplicationLink,listCandidateApplicationLinks} from '@/lib/recruiting/candidate-portal-service';
export async function GET(r:Request,c:{params:Promise<{orgId:string}>}){try{const{orgId}=await c.params,a=await actorFromRequest(r,orgId);requirePermission(a,'recruiting.read');return NextResponse.json({data:await listCandidateApplicationLinks(a)})}catch(e){return apiErrorResponse(e)}}
export async function POST(r:Request,c:{params:Promise<{orgId:string}>}){try{const{orgId}=await c.params,a=await actorFromRequest(r,orgId);requirePermission(a,'recruiting.manage');return NextResponse.json({data:await createCandidateApplicationLink(a,await r.json())},{status:201})}catch(e){return apiErrorResponse(e)}}

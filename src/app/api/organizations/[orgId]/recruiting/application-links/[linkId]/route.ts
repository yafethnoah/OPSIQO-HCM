import {NextResponse} from 'next/server';import {actorFromRequest,requirePermission} from '@/lib/auth/session';import {apiErrorResponse} from '@/lib/http/errors';import {updateCandidateApplicationLink} from '@/lib/recruiting/candidate-portal-service';
export async function PATCH(r:Request,c:{params:Promise<{orgId:string;linkId:string}>}){try{const{orgId,linkId}=await c.params,a=await actorFromRequest(r,orgId);requirePermission(a,'recruiting.manage');return NextResponse.json({data:await updateCandidateApplicationLink(a,linkId,await r.json())})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server'; import { actorFromRequest, requirePermission } from '@/lib/auth/session'; import { apiErrorResponse } from '@/lib/http/errors'; import { actOnOffer } from '@/lib/recruiting/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;offerId:string}>}){try{const{orgId,offerId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'recruiting.offer');return NextResponse.json({data:await actOnOffer(actor,offerId,await request.json())});}catch(e){return apiErrorResponse(e);}}

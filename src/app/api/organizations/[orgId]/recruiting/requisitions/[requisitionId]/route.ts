import { NextResponse } from 'next/server'; import { actorFromRequest, requirePermission } from '@/lib/auth/session'; import { apiErrorResponse } from '@/lib/http/errors'; import { actOnRequisition } from '@/lib/recruiting/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;requisitionId:string}>}){try{const{orgId,requisitionId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'recruiting.manage');return NextResponse.json({data:await actOnRequisition(actor,requisitionId,await request.json())});}catch(e){return apiErrorResponse(e);}}

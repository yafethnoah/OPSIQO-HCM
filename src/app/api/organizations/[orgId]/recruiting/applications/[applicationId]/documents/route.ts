import {NextResponse} from 'next/server';import {actorFromRequest,requirePermission} from '@/lib/auth/session';import {apiErrorResponse} from '@/lib/http/errors';import {listCandidateApplicationDocuments} from '@/lib/recruiting/candidate-documents';
export async function GET(r:Request,c:{params:Promise<{orgId:string;applicationId:string}>}){try{const{orgId,applicationId}=await c.params,a=await actorFromRequest(r,orgId);requirePermission(a,'recruiting.read');return NextResponse.json({data:await listCandidateApplicationDocuments(a,applicationId)})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server'; import { actorFromRequest, requirePermission } from '@/lib/auth/session'; import { apiErrorResponse } from '@/lib/http/errors'; import { submitScorecard } from '@/lib/recruiting/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;interviewId:string}>}){try{const{orgId,interviewId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'recruiting.interview');return NextResponse.json({data:await submitScorecard(actor,interviewId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

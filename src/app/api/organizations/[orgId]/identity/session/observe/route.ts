import { NextResponse } from 'next/server';import { actorFromRequest,identityFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { observeIdentitySession } from '@/lib/identity/service';export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const identity=await identityFromRequest(request);const actor=await actorFromRequest(request,orgId);requirePermission(actor,'identity.read');return NextResponse.json({data:await observeIdentitySession(actor,identity)})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actImpactAnalysis } from '@/lib/resilience/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;analysisId:string}>}){try{const{orgId,analysisId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'resilience.manage');return NextResponse.json({data:await actImpactAnalysis(actor,analysisId,await request.json())})}catch(e){return apiErrorResponse(e)}}
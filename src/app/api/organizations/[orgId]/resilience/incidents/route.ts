import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createCrisisIncident } from '@/lib/resilience/service';export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'resilience.incident');return NextResponse.json({data:await createCrisisIncident(actor,await request.json())},{status:201})}catch(e){return apiErrorResponse(e)}}
import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actRecoveryPlan } from '@/lib/resilience/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;recoveryId:string}>}){try{const{orgId,recoveryId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'resilience.manage');return NextResponse.json({data:await actRecoveryPlan(actor,recoveryId,await request.json())})}catch(e){return apiErrorResponse(e)}}
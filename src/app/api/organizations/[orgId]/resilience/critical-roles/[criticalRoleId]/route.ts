import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actCriticalRole } from '@/lib/resilience/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;criticalRoleId:string}>}){try{const{orgId,criticalRoleId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'resilience.manage');return NextResponse.json({data:await actCriticalRole(actor,criticalRoleId,await request.json())})}catch(e){return apiErrorResponse(e)}}
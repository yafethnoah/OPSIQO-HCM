import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actExercise } from '@/lib/resilience/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;exerciseId:string}>}){try{const{orgId,exerciseId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'resilience.manage');return NextResponse.json({data:await actExercise(actor,exerciseId,await request.json())})}catch(e){return apiErrorResponse(e)}}
import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actRiskAppetite } from '@/lib/strategy/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;appetiteId:string}>}){try{const{orgId,appetiteId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'strategy.manage');return NextResponse.json({data:await actRiskAppetite(actor,appetiteId,await request.json())})}catch(e){return apiErrorResponse(e)}}
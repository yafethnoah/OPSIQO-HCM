import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actScenario } from '@/lib/strategy/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;scenarioId:string}>}){try{const{orgId,scenarioId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'strategy.manage');return NextResponse.json({data:await actScenario(actor,scenarioId,await request.json())})}catch(e){return apiErrorResponse(e)}}
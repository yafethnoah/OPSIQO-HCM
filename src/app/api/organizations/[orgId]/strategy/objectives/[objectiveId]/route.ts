import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actObjective } from '@/lib/strategy/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;objectiveId:string}>}){try{const{orgId,objectiveId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'strategy.manage');return NextResponse.json({data:await actObjective(actor,objectiveId,await request.json())})}catch(e){return apiErrorResponse(e)}}
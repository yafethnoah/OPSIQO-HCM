import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actStrategyReport } from '@/lib/strategy/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;reportId:string}>}){try{const{orgId,reportId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'strategy.manage');return NextResponse.json({data:await actStrategyReport(actor,reportId,await request.json())})}catch(e){return apiErrorResponse(e)}}
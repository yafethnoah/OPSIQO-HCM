import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actInitiative } from '@/lib/strategy/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;initiativeId:string}>}){try{const{orgId,initiativeId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'strategy.manage');return NextResponse.json({data:await actInitiative(actor,initiativeId,await request.json())})}catch(e){return apiErrorResponse(e)}}
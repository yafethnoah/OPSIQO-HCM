import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actCapabilityGap } from '@/lib/strategy/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;gapId:string}>}){try{const{orgId,gapId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'strategy.manage');return NextResponse.json({data:await actCapabilityGap(actor,gapId,await request.json())})}catch(e){return apiErrorResponse(e)}}
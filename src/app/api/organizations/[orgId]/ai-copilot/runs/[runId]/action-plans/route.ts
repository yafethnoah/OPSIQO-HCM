import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createActionPlan } from '@/lib/ai-intelligence/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;runId:string}>}){try{const{orgId,runId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'ai.use');return NextResponse.json({data:await createActionPlan(actor,runId,await request.json())});}catch(e){return apiErrorResponse(e);}}

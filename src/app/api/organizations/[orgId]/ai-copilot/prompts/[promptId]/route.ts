import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actPrompt } from '@/lib/ai-intelligence/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;promptId:string}>}){try{const{orgId,promptId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'ai.approve');return NextResponse.json({data:await actPrompt(actor,promptId,await request.json())});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actActionPlan } from '@/lib/ai-intelligence/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;planId:string}>}){try{const{orgId,planId}=await context.params;const actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await actActionPlan(actor,planId,await request.json())});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { addEvidence } from '@/lib/employee-relations/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;caseId:string}>}){try{const{orgId,caseId}=await context.params;return NextResponse.json({data:await addEvidence(await actorFromRequest(request,orgId),caseId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

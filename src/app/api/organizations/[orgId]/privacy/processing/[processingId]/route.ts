import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnProcessingActivity } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;processingId:string}>}){try{const{orgId,processingId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnProcessingActivity(actor,processingId,await request.json())});}catch(e){return apiErrorResponse(e);}}

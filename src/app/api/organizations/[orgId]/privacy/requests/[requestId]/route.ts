import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPrivacyRequest } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;requestId:string}>}){try{const{orgId,requestId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnPrivacyRequest(actor,requestId,await request.json())});}catch(e){return apiErrorResponse(e);}}

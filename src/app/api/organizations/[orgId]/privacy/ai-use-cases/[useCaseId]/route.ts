import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPrivacyAiUseCase } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;useCaseId:string}>}){try{const{orgId,useCaseId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnPrivacyAiUseCase(actor,useCaseId,await request.json())});}catch(e){return apiErrorResponse(e);}}

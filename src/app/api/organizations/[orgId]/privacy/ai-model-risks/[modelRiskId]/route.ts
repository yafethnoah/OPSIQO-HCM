import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPrivacyAiModelRisk } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;modelRiskId:string}>}){try{const{orgId,modelRiskId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnPrivacyAiModelRisk(actor,modelRiskId,await request.json())});}catch(e){return apiErrorResponse(e);}}

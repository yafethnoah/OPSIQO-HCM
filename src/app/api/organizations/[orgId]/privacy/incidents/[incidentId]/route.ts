import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPrivacyIncident } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;incidentId:string}>}){try{const{orgId,incidentId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnPrivacyIncident(actor,incidentId,await request.json())});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPrivacyAssessment } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;assessmentId:string}>}){try{const{orgId,assessmentId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnPrivacyAssessment(actor,assessmentId,await request.json())});}catch(e){return apiErrorResponse(e);}}

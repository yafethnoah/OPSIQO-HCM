import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnRetentionSchedule } from '@/lib/privacy/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;retentionId:string}>}){try{const{orgId,retentionId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'privacy.manage');return NextResponse.json({data:await actOnRetentionSchedule(actor,retentionId,await request.json())});}catch(e){return apiErrorResponse(e);}}

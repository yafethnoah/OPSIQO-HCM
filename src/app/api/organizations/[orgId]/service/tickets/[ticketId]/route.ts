import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actServiceTicket } from '@/lib/experience/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;ticketId:string}>}){try{const{orgId,ticketId}=await context.params;const actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await actServiceTicket(actor,ticketId,await request.json())});}catch(e){return apiErrorResponse(e);}}

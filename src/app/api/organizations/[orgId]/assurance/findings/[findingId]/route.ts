import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnAssuranceFinding } from '@/lib/assurance/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;findingId:string}>}){try{const{orgId,findingId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'assurance.manage');return NextResponse.json({data:await actOnAssuranceFinding(actor,findingId,await request.json())});}catch(e){return apiErrorResponse(e);}}

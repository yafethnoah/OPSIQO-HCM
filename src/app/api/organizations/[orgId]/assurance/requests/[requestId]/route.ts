import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnEvidenceRequest } from '@/lib/assurance/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;requestId:string}>}){try{const{orgId,requestId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'assurance.manage');return NextResponse.json({data:await actOnEvidenceRequest(actor,requestId,await request.json())});}catch(e){return apiErrorResponse(e);}}

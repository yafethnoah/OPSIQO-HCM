import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPerformanceCycle } from '@/lib/performance/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;cycleId:string}>}){try{const{orgId,cycleId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'performance.manage');return NextResponse.json({data:await actOnPerformanceCycle(actor,cycleId,await request.json())});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { submitFeedbackResponse } from '@/lib/performance/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;requestId:string}>}){try{const{orgId,requestId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'performance.read');return NextResponse.json({data:await submitFeedbackResponse(actor,requestId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnPip } from '@/lib/performance/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;pipId:string}>}){try{const{orgId,pipId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'performance.pip');return NextResponse.json({data:await actOnPip(actor,pipId,await request.json())});}catch(e){return apiErrorResponse(e);}}

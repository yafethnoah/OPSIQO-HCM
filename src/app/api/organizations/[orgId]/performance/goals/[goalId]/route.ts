import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { updatePerformanceGoal } from '@/lib/performance/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;goalId:string}>}){try{const{orgId,goalId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'performance.review');return NextResponse.json({data:await updatePerformanceGoal(actor,goalId,await request.json())});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createPerformanceGoal } from '@/lib/performance/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);if(!actor.permissions.includes('performance.review')&&!actor.permissions.includes('performance.manage'))requirePermission(actor,'performance.review');return NextResponse.json({data:await createPerformanceGoal(actor,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

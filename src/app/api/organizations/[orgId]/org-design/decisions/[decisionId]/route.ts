import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actDecisionRight } from '@/lib/org-design/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;decisionId:string}>}){try{const{orgId,decisionId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'orgdesign.manage');return NextResponse.json({data:await actDecisionRight(actor,decisionId,await request.json())})}catch(e){return apiErrorResponse(e)}}

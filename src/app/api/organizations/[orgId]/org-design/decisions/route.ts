import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createDecisionRight } from '@/lib/org-design/service';export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'orgdesign.manage');return NextResponse.json({data:await createDecisionRight(actor,await request.json())},{status:201})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actKpi } from '@/lib/org-design/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;kpiId:string}>}){try{const{orgId,kpiId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'orgdesign.manage');return NextResponse.json({data:await actKpi(actor,kpiId,await request.json())})}catch(e){return apiErrorResponse(e)}}

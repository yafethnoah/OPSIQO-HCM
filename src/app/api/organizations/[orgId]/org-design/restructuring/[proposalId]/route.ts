import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actRestructuring } from '@/lib/org-design/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;proposalId:string}>}){try{const{orgId,proposalId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'orgdesign.manage');return NextResponse.json({data:await actRestructuring(actor,proposalId,await request.json())})}catch(e){return apiErrorResponse(e)}}

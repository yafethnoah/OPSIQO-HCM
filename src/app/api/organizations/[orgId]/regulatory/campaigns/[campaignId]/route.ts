import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnReattestationCampaign } from '@/lib/regulatory/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;campaignId:string}>}){try{const{orgId,campaignId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'regulatory.manage');return NextResponse.json({data:await actOnReattestationCampaign(actor,campaignId,await request.json())});}catch(e){return apiErrorResponse(e);}}

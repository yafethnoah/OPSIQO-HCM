import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createPolicyRevisionFromImpact } from '@/lib/regulatory/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;impactId:string}>}){try{const{orgId,impactId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'regulatory.manage');requirePermission(actor,'policies.manage');return NextResponse.json({data:await createPolicyRevisionFromImpact(actor,impactId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

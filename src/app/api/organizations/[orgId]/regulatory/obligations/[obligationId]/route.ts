import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnRegulatoryObligation } from '@/lib/regulatory/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;obligationId:string}>}){try{const{orgId,obligationId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'regulatory.manage');return NextResponse.json({data:await actOnRegulatoryObligation(actor,obligationId,await request.json())});}catch(e){return apiErrorResponse(e);}}

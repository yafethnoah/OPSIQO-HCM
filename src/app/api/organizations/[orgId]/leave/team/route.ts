import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { listTeamLeave } from '@/lib/time/service';
export async function GET(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'leave.approve');const u=new URL(request.url);return NextResponse.json({data:await listTeamLeave(actor,u.searchParams.get('start')||undefined,u.searchParams.get('end')||undefined)});}catch(e){return apiErrorResponse(e);}}

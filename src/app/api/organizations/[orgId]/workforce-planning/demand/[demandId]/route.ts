import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnWorkforceDemand } from '@/lib/workforce-planning/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;demandId:string}>}){try{const{orgId,demandId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'workforce.manage');return NextResponse.json({data:await actOnWorkforceDemand(actor,demandId,await request.json())});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnCourse } from '@/lib/learning/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;courseId:string}>}){try{const{orgId,courseId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'learning.manage');return NextResponse.json({data:await actOnCourse(actor,courseId,await request.json())});}catch(e){return apiErrorResponse(e);}}

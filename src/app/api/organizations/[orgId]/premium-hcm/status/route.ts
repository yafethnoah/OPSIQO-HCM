import{NextResponse}from'next/server';import{actorFromRequest,requirePermission}from'@/lib/auth/session';import{apiErrorResponse}from'@/lib/http/errors';import{premiumHcmRecommendations,evidenceWeights,premiumImplementationCoverage}from'@/lib/premium-hcm/registry';export async function GET(r:Request,c:{params:Promise<{orgId:string}>}){try{const{orgId}=await c.params,a=await actorFromRequest(r,orgId);requirePermission(a,'platform.read');return NextResponse.json({data:{recommendations:premiumHcmRecommendations,evidenceWeights,coverage:premiumImplementationCoverage(),certificationRule:'MODULE_STATUS=CERTIFIED only after every mandatory evidence gate passes.',regulatedPayrollRule:'Native payroll implementation must not be promoted as production-certified until independent authoritative reconciliation passes.'}});}catch(e){return apiErrorResponse(e)}}

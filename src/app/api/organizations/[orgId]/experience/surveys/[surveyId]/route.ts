import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actSurvey,saveSurvey } from '@/lib/experience/service';
export async function PUT(request:Request,context:{params:Promise<{orgId:string;surveyId:string}>}){try{const{orgId,surveyId}=await context.params;const actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await saveSurvey(actor,await request.json(),surveyId)});}catch(e){return apiErrorResponse(e);}}
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;surveyId:string}>}){try{const{orgId,surveyId}=await context.params;const actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await actSurvey(actor,surveyId,await request.json())});}catch(e){return apiErrorResponse(e);}}

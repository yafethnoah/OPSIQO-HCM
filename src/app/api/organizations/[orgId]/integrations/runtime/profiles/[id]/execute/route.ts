import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { executeProfile } from '@/lib/integration/runtime-service';export async function POST(request:Request,context:{params:Promise<{orgId:string;id:string}>}){try{const{orgId,id}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'integration.manage');const body=await request.json().catch(()=>({}));const mode=body?.mode==='test'?'test':'manual';return NextResponse.json({data:await executeProfile(actor,id,mode)})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnForecastRun } from '@/lib/people-analytics/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;runId:string}>}){try{const{orgId,runId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'peopleanalytics.manage');return NextResponse.json({data:await actOnForecastRun(actor,runId,await request.json())});}catch(e){return apiErrorResponse(e);}}

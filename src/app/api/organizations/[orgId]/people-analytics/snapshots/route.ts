import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { generateAnalyticsSnapshot } from '@/lib/people-analytics/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'peopleanalytics.manage');return NextResponse.json({data:await generateAnalyticsSnapshot(orgId,actor.uid)});}catch(e){return apiErrorResponse(e);}}

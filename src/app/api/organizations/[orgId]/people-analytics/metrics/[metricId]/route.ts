import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnAnalyticsMetric } from '@/lib/people-analytics/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;metricId:string}>}){try{const{orgId,metricId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'peopleanalytics.manage');return NextResponse.json({data:await actOnAnalyticsMetric(actor,metricId,await request.json())});}catch(e){return apiErrorResponse(e);}}

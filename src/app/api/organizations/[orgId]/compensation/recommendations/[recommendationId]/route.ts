import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actRecommendation } from '@/lib/compensation/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;recommendationId:string}>}){try{const{orgId,recommendationId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'compensation.read');return NextResponse.json({data:await actRecommendation(actor,recommendationId,await request.json())});}catch(e){return apiErrorResponse(e);}}

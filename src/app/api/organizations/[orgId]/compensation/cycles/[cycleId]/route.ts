import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actCompensationCycleControlled } from '@/lib/compensation/controls';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;cycleId:string}>}){try{const{orgId,cycleId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'compensation.manage');return NextResponse.json({data:await actCompensationCycleControlled(actor,cycleId,await request.json())});}catch(e){return apiErrorResponse(e);}}

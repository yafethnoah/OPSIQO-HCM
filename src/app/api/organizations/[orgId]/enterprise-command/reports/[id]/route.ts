import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actReport } from '@/lib/enterprise-command/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;id:string}>}){try{const{orgId,id}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'commandcenter.manage');return NextResponse.json({data:await actReport(actor,id,await request.json())})}catch(e){return apiErrorResponse(e)}}

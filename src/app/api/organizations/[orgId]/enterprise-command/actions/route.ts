import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createAction } from '@/lib/enterprise-command/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'commandcenter.manage');return NextResponse.json({data:await createAction(actor,await request.json())},{status:201})}catch(e){return apiErrorResponse(e)}}

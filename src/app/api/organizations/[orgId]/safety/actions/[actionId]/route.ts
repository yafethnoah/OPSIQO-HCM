import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { updateSafetyCorrectiveAction } from '@/lib/safety/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;actionId:string}>}){try{const{orgId,actionId}=await context.params;const actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await updateSafetyCorrectiveAction(actor,actionId,await request.json())});}catch(e){return apiErrorResponse(e);}}

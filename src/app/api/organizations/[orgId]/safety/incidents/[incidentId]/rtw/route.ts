import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { upsertReturnToWorkPlan } from '@/lib/safety/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;incidentId:string}>}){try{const{orgId,incidentId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'safety.rtw');return NextResponse.json({data:await upsertReturnToWorkPlan(actor,incidentId,await request.json())});}catch(e){return apiErrorResponse(e);}}

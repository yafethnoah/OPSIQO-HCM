import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { getSafetyIncident,updateSafetyIncident } from '@/lib/safety/service';
export async function GET(request:Request,context:{params:Promise<{orgId:string;incidentId:string}>}){try{const{orgId,incidentId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'safety.report');return NextResponse.json({data:await getSafetyIncident(actor,incidentId)});}catch(e){return apiErrorResponse(e);}}
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;incidentId:string}>}){try{const{orgId,incidentId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'safety.manage');return NextResponse.json({data:await updateSafetyIncident(actor,incidentId,await request.json())});}catch(e){return apiErrorResponse(e);}}

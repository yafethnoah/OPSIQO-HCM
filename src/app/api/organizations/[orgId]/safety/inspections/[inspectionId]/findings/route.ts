import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { addInspectionFinding } from '@/lib/safety/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;inspectionId:string}>}){try{const{orgId,inspectionId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'safety.manage');return NextResponse.json({data:await addInspectionFinding(actor,inspectionId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}

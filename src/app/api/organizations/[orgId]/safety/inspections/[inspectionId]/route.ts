import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { updateSafetyInspection } from '@/lib/safety/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;inspectionId:string}>}){try{const{orgId,inspectionId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'safety.manage');return NextResponse.json({data:await updateSafetyInspection(actor,inspectionId,await request.json())});}catch(e){return apiErrorResponse(e);}}

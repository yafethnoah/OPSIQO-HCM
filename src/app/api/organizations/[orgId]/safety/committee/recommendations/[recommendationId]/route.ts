import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { respondSafetyCommitteeRecommendation } from '@/lib/safety/service';
export async function PATCH(request:Request,context:{params:Promise<{orgId:string;recommendationId:string}>}){try{const{orgId,recommendationId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'safety.committee');return NextResponse.json({data:await respondSafetyCommitteeRecommendation(actor,recommendationId,await request.json())});}catch(e){return apiErrorResponse(e);}}

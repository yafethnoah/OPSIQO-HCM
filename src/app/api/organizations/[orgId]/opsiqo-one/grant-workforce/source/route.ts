import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createGrantFundingSource } from '@/lib/opsiqo-one/grant-workforce';export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await createGrantFundingSource(actor,await request.json())},{status:201})}catch(e){return apiErrorResponse(e)}}

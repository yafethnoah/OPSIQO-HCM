import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { organizationalMemory } from '@/lib/opsiqo-one/organizational-memory';
export async function GET(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId),q=new URL(request.url).searchParams.get('q')||'';return NextResponse.json({data:await organizationalMemory(actor,q)});}catch(e){return apiErrorResponse(e)}}

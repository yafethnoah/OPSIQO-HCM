import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createCustomAgent,listCustomAgents } from '@/lib/opsiqo-one/agent-builder';
export async function GET(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId);if(!actor.permissions.includes('ai.manage')&&!actor.permissions.includes('ai.audit'))requirePermission(actor,'ai.manage');return NextResponse.json({data:await listCustomAgents(actor)});}catch(e){return apiErrorResponse(e)}}
export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId);requirePermission(actor,'ai.manage');return NextResponse.json({data:await createCustomAgent(actor,await request.json())},{status:201});}catch(e){return apiErrorResponse(e)}}

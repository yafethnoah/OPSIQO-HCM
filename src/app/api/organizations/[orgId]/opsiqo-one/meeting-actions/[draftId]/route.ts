import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actMeetingActionDraft } from '@/lib/opsiqo-one/meeting-actions';export async function POST(request:Request,context:{params:Promise<{orgId:string;draftId:string}>}){try{const{orgId,draftId}=await context.params,actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await actMeetingActionDraft(actor,draftId,await request.json())})}catch(e){return apiErrorResponse(e)}}

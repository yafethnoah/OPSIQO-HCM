import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { promoteMeetingActionsToWorkflow } from '@/lib/opsiqo-one/meeting-actions';export async function POST(request:Request,context:{params:Promise<{orgId:string;draftId:string}>}){try{const{orgId,draftId}=await context.params,actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await promoteMeetingActionsToWorkflow(actor,draftId,await request.json())},{status:201})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { accessibilityReadiness,translationCoverage } from '@/lib/opsiqo-one/experience-readiness';export const dynamic='force-dynamic';export async function GET(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId);return NextResponse.json({data:{accessibility:accessibilityReadiness(actor),translation:translationCoverage(actor)}},{headers:{'Cache-Control':'private, no-store'}})}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { programPortfolioDashboard } from '@/lib/opsiqo-one/program-portfolio';export const dynamic='force-dynamic';export async function GET(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await programPortfolioDashboard(actor)},{headers:{'Cache-Control':'private, no-store'}})}catch(e){return apiErrorResponse(e)}}

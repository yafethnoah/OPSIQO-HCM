import { NextResponse } from 'next/server';import { actorFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { recordProgramFinancialEvidence } from '@/lib/opsiqo-one/program-portfolio';export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params,actor=await actorFromRequest(request,orgId);return NextResponse.json({data:await recordProgramFinancialEvidence(actor,await request.json())},{status:201})}catch(e){return apiErrorResponse(e)}}

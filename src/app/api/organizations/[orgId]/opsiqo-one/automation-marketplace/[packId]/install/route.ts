import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { installAutomationPack } from '@/lib/opsiqo-one/automation-marketplace';
export async function POST(request:Request,context:{params:Promise<{orgId:string;packId:string}>}){try{const{orgId,packId}=await context.params,actor=await actorFromRequest(request,orgId);requirePermission(actor,'workflow.manage');return NextResponse.json({data:await installAutomationPack(actor,packId)},{status:201});}catch(e){return apiErrorResponse(e)}}

import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { analyzeDiagnosticControlWithAi } from '@/lib/hr-diagnostic/service';export async function POST(request:Request,context:{params:Promise<{orgId:string;assessmentId:string;controlId:string}>}){try{const{orgId,assessmentId,controlId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'diagnostic.read');return NextResponse.json({data:await analyzeDiagnosticControlWithAi(actor,assessmentId,controlId)});}catch(e){return apiErrorResponse(e);}}
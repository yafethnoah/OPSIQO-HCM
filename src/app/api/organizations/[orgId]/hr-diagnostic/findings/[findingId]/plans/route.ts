import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { createDiagnosticRemediationPlan } from '@/lib/hr-diagnostic/service';export async function POST(request:Request,context:{params:Promise<{orgId:string;findingId:string}>}){try{const{orgId,findingId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'diagnostic.manage');return NextResponse.json({data:await createDiagnosticRemediationPlan(actor,findingId,await request.json())},{status:201});}catch(e){return apiErrorResponse(e);}}
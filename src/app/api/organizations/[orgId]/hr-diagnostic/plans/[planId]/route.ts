import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnDiagnosticRemediationPlan } from '@/lib/hr-diagnostic/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;planId:string}>}){try{const{orgId,planId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'diagnostic.manage');return NextResponse.json({data:await actOnDiagnosticRemediationPlan(actor,planId,await request.json())});}catch(e){return apiErrorResponse(e);}}
import { NextResponse } from 'next/server';import { actorFromRequest,requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { actOnDiagnosticRemediationTask } from '@/lib/hr-diagnostic/service';export async function PATCH(request:Request,context:{params:Promise<{orgId:string;planId:string;taskId:string}>}){try{const{orgId,planId,taskId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'diagnostic.manage');return NextResponse.json({data:await actOnDiagnosticRemediationTask(actor,planId,taskId,await request.json())});}catch(e){return apiErrorResponse(e);}}
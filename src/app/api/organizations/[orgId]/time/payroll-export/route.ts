import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { exportPayrollCsv } from '@/lib/time/service';
export async function GET(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'payroll.export');const u=new URL(request.url),start=u.searchParams.get('periodStart')||'',end=u.searchParams.get('periodEnd')||'';const r=await exportPayrollCsv(actor,start,end);return new Response(r.csv,{headers:{'content-type':'text/csv; charset=utf-8','content-disposition':`attachment; filename="opsiqo-payroll-${start}-${end}.csv"`,'cache-control':'no-store, max-age=0','cdn-cache-control':'no-store'}});}catch(e){return apiErrorResponse(e);}}

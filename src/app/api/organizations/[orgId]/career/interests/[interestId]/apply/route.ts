import { NextResponse } from 'next/server';import { actorFromRequest, requirePermission } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { applyInternalOpportunity } from '@/lib/career/service';
export async function POST(request:Request,context:{params:Promise<{orgId:string;interestId:string}>}){try{const{orgId,interestId}=await context.params;const actor=await actorFromRequest(request,orgId);requirePermission(actor,'career.read');return NextResponse.json({data:await applyInternalOpportunity(actor,interestId)});}catch(e){return apiErrorResponse(e);}}

import { NextResponse } from 'next/server';import { apiErrorResponse } from '@/lib/http/errors';import { publicSsoProviders } from '@/lib/identity/service';export const dynamic='force-dynamic';export async function GET(request:Request){try{const orgId=new URL(request.url).searchParams.get('orgId')||'';if(!/^[A-Za-z0-9._-]{2,200}$/.test(orgId))return NextResponse.json({data:[]});return NextResponse.json({data:await publicSsoProviders(orgId)},{headers:{'Cache-Control':'no-store'}})}catch(e){return apiErrorResponse(e)}}

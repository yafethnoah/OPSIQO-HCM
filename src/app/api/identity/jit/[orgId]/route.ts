import { NextResponse } from 'next/server';import { identityFromRequest } from '@/lib/auth/session';import { apiErrorResponse } from '@/lib/http/errors';import { processJitIdentity } from '@/lib/identity/service';export async function POST(request:Request,context:{params:Promise<{orgId:string}>}){try{const{orgId}=await context.params;const identity=await identityFromRequest(request);return NextResponse.json({data:await processJitIdentity(request,orgId,identity)})}catch(e){return apiErrorResponse(e)}}

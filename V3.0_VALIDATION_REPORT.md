# OPSIQO HCM v3.0 Validation Report

Release: **3.0.0 — Enterprise HCM Command Center & Production Integration**  
Validation date: **2026-08-11**

## Source / integration validation
- TypeScript / TSX files parsed under `src`, `scripts`, and `tests`: **591**
- TypeScript parser diagnostics: **0**
- Project-local imports inspected: **1,590**
- Unresolved project-local imports: **0**
- Command Center API route files: **10**
- Command Center Firestore collections/indexes explicitly server-only: **7 / 7**
- Firestore Rules brace structure: **508 / 508 PASS**
- Storage Rules brace structure: **5 / 5 PASS**
- `package.json`, `firebase.json`, `firestore.indexes.json`, and `tsconfig.json`: **JSON PASS**
- `.github/workflows/production-promotion.yml`: **YAML PASS**
- Targeted v3.0 TypeScript scan after excluding diagnostics caused solely by unavailable external dependency typings: **0 material v3.0 diagnostics**

## Governance / runtime checks
- Healthy Enterprise HCM profile: **93 / 100 — Strategic**
- High-risk / overdue profile: **29 / 100 — Fragile**
- Deterministic Low/Critical domain-risk thresholds: **PASS**
- Higher-is-better and lower-is-better SLO breach logic: **PASS**
- Production release without traceable CI evidence: **BLOCKED — PASS**
- Production release with all mandatory gates + CI evidence: **READY — PASS**
- Staging release with all mandatory gates: **READY — PASS**
- HR Partner: read/manage/audit but no `commandcenter.approve`: **PASS**
- HR Admin / Org Admin approval permission: **PASS**
- Manager / Employee enterprise Command Center access: **DENIED by RBAC — PASS**
- Cross-module action completion requires approved/in-progress state and completed/cancelled tasks: **PASS (source gate)**
- Command Center route/service scan contains no direct recruiting, separation, compensation, succession or comparable consequential worker-mutation service import: **PASS**
- AI integration uses aggregate Command Center evidence and remains permission-scoped by `commandcenter.read`: **PASS (source gate)**
- Lifecycle integrated UAT includes `/enterprise-command/dashboard`: **PASS**

## Release-engineering controls
The v3.0 release gate now checks the actual **3.0.0** release rather than the inherited stale version condition found in the v2.9 package.

Static filesystem/source release-gate status before dependency installation:
- **10 / 11 PASS**
- The sole failing gate is the required trusted `package-lock.json`.

The protected/manual production-promotion workflow requires:
1. `npm ci`
2. `npm run release:gate`
3. `npm run typecheck`
4. `npm test`
5. `npm run test:rules`
6. `npm run build`
7. `npm run ai:evaluate`
8. `npm run ai:governance-check`
9. `npm run preflight:production`
10. authenticated `npm run uat:lifecycle` against the deployment candidate

The workflow is a **promotion gate**, not an automatic deployment mechanism.

## Dependency-aware build status
A bounded public-registry command was attempted:

```powershell
npm install --package-lock-only --ignore-scripts --registry=https://registry.npmjs.org --no-audit --no-fund
```

It exceeded the available execution window and returned timeout status **124**. It produced **no `package-lock.json` and no `node_modules` tree**. No partial dependency state is included in the release.

Therefore v3.0 is **source/integration validated, not dependency-build verified in this environment**. Do not promote to production until the complete dependency-aware acceptance sequence passes in the approved CI/Windows environment.

## Decision boundary
The Enterprise HCM Index, domain risk nodes, data-quality snapshots, SLOs, release assessments, cross-module actions, and Board snapshots are operational governance artifacts. They do not constitute legal advice, compliance certification, audit opinion, financial forecast, or authority to make individual consequential employment decisions.

## Source-manifest integrity
The final `SOURCE_MANIFEST.sha256` is generated only after this report and all shipped source/release documents are frozen. Verify it with a SHA-256 tool before production promotion.

Final release files before source manifest: **667**.

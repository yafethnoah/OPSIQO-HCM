import fs from 'node:fs';import path from 'node:path';
const ledgerArg=process.argv[2];const root=process.cwd();const artifactDir=path.join(root,'artifacts');
const ledgerCandidates=[ledgerArg,path.join(artifactDir,'v7-29-certification-ledger.json'),path.join(process.env.TEMP||process.env.TMP||'/tmp','opsiqo-v7-29-certification-ledger.json')].filter(Boolean);
const ledgerPath=ledgerCandidates.find(p=>fs.existsSync(p));const load=p=>{try{return JSON.parse(fs.readFileSync(p,'utf8'))}catch{return null}};const ledger=ledgerPath?load(ledgerPath):null;const gates=Array.isArray(ledger?.gates)?ledger.gates:[];const passed=name=>gates.some(g=>g.name===name&&g.status==='pass');const allPassed=names=>names.every(passed);
const human=load(path.join(artifactDir,'v7-29-human-signoff.json'));const approval=x=>Boolean(x?.approved===true&&String(x?.reviewedBy||'').trim()&&String(x?.reviewedAt||'').trim());
const sourceCertified=allPassed(['V7.29 Final Release Attestation audit','V7.29 translation inventory verification','Final frozen source verification']);
const dependencyCertified=allPassed(['Locked dependency installation','Locked toolchain post-install preflight','Semantic TypeScript','Full Vitest suite','Firestore Rules isolation','Security static scan','Production Next.js build','Release gate','Final production rebuild after emulator UAT','Final release gate after production rebuild']);
const browserUatCertified=allPassed(['Built-app public browser accessibility smoke','Authenticated emulator-backed accessibility UAT']);
const manualAccessibilityCertified=approval(human?.manualAccessibility);const connectorUatCertified=approval(human?.connectorUat);const releaseChangeApproval=approval(human?.releaseChangeApproval);const productionDeploymentSignoff=approval(human?.productionDeployment);
const productionCandidate=sourceCertified&&dependencyCertified&&browserUatCertified;const readyForHumanSignoff=productionCandidate&&manualAccessibilityCertified&&connectorUatCertified&&releaseChangeApproval;const productionDeployed=readyForHumanSignoff&&productionDeploymentSignoff;
const report={version:'7.29',generatedAt:new Date().toISOString(),ledger:ledgerPath?path.basename(ledgerPath):null,status:{sourceCertified,dependencyCertified,browserUatCertified,manualAccessibilityCertified,connectorUatCertified,releaseChangeApproval,productionCandidate,readyForHumanSignoff,productionDeployed},boundary:'This report separates source, dependency, browser-UAT, manual accessibility, connector-UAT, release-change approval and production-deployment evidence. It never deploys OPSIQO, never reads .env files, never inspects credential values, and never treats automated browser checks as a formal WCAG conformance sign-off.',requiredHumanEvidence:'artifacts/v7-29-human-signoff.json',template:'PRODUCTION_SIGNOFF_TEMPLATE_V7_29.json'};
fs.mkdirSync(artifactDir,{recursive:true});fs.writeFileSync(path.join(artifactDir,'v7-29-deployment-readiness.json'),JSON.stringify(report,null,2));console.log('\nOPSIQO ONE V7.29 DEPLOYMENT READINESS');for(const[k,v]of Object.entries(report.status))console.log(`${v?'PASS':'PENDING'} ${k}`);console.log('Boundary: certification evidence is separated from manual sign-off and production deployment. No deployment action is performed.');console.log('Evidence: artifacts/v7-29-deployment-readiness.json');
